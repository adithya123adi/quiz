
const host = 'http://192.168.0.105:1337'

const constants = {
    url: host,
    dropdown: host + '/dropdowns',
    icons: host + '/icons',
    subject: host + '/subjects'
}

export default constants;